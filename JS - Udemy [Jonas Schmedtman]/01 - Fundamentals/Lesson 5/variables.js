// Variable can be changed, can also be empty
let age = 33;
age = 33;

// Variable can't be changed. Const can't be empy. Use const at default if it shouldn't be changed
const birthYear = 1993;
// birthYear = 1990; this doesn't work

// Var should be avoided
var oldWay = "deprecated";
oldWay = "But still exists";