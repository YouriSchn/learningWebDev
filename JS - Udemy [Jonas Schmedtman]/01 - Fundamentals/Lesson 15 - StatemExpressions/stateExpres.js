// Expression is a piece of code that produces a value

3 * 4 // is an expression, it produces a value
1993 // also an expession
true && false && !false; // also a value, so an expression


// This is a statement, as it doesn't produce a value
if (23 > 10) {
  const str = '23 is bigger'; // the string is a expression
} 